{
  "name": "app-blueprint",
  "version": "0.1.19",
  "description": "Cross-platform desktop UI app with Electron and TypeScript",
  "main": "dist/main.js",
  "author": "Stephan Praetsch <noreply@example.com>",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=26.x"
  },
  "scripts": {
    "build": "tsc",
    "dev": "yarn build && electron .",
    "start": "electron .",
    "dist": "yarn build && electron-builder --publish=never",
    "dist:linux": "yarn build && electron-builder --linux AppImage deb --publish=never",
    "dist:linux:appimage": "yarn build && electron-builder --linux AppImage --publish=never",
    "dist:linux:deb": "yarn build && electron-builder --linux deb --publish=never",
    "dist:win": "yarn build && electron-builder --win nsis --publish=never",
    "dist:mac": "yarn build && electron-builder --mac dmg --universal --publish=never"
  },
  "devDependencies": {
    "@electron/notarize": "3.1.0",
    "@types/node": "24.13.2",
    "electron": "43.0.0",
    "electron-builder": "26.15.3",
    "typescript": "5.9.3"
  },
  "build": {
    "appId": "com.example.appblueprint",
    "productName": "App Blueprint",
    "afterSign": "scripts/notarize.cjs",
    "compression": "maximum",
    "electronLanguages": [
      "en-US",
      "de"
    ],
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "index.html",
      "package.json"
    ],
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Utility"
    },
    "win": {
      "target": [
        "nsis"
      ]
    },
    "mac": {
      "target": [
        {
          "target": "dmg",
          "arch": [
            "universal"
          ]
        }
      ],
      "category": "public.app-category.utilities",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.inherit.plist"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "packageManager": "yarn@1.22.22"
}
